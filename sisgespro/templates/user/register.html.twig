{% extends 'base.html.twig' %}

{% block title %}

    {% if edit is defined %}
        Editar Usuario
    {% else %}
        Registro de Usario
    {% endif %}

{% endblock %}

{% block body %}

{{parent()}}

<div class="example-wrapper">

     <h2 class='title'>
            {% if edit is defined %}
                Editar Usuario
            {% else %}
                Registro de Usario
            {% endif %}
     </h2>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    
</div>

{% endblock %}